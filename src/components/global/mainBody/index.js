'use client';

import React from 'react';
import { Box } from '@mui/material';

function MainBody() {
    return (
        <Box sx={{
            backgroundColor: '#fcba03',
            width: '100%',
            height: '100%',
        }}>
        </Box>
    );
};

export default MainBody;

